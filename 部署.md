# 部署

面向一台公网 Linux、Python 3.11+、无额外应用权限的机器。Windows 本机同样用 `python -m mint_engine`。

## 1. 安装

```bash
sudo apt-get update
sudo apt-get install -y python3.11 python3.11-venv python3-pip
git clone https://github.com/Dog-Feng/mint-bot.git /opt/mint-bot
cd /opt/mint-bot
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

编辑 `.env`：

```env
HOST=0.0.0.0
PORT=8056
OPENSEA_API_KEY=你的OpenSea Key
ETHERSCAN_API_KEY=你的Etherscan Key
```

目录要可写：`abi_cache/`、`results/`（进程会自己建）。不要把 `.env` 拷到镜像或提交 git。

## 2. 启动与停止

前台：

```bash
cd /opt/mint-bot
source .venv/bin/activate
python -m mint_engine
```

Windows CMD：`cd /d 项目目录` 后 `python -m mint_engine`（有 venv 则先 `.venv\Scripts\activate.bat`）。

日志里出现 `listening on http://0.0.0.0:8056 (no auth)` 即成功。探活：

```bash
curl -s http://127.0.0.1:8056/api/health
# {"ok":true}
```

停止：对前台进程 `Ctrl+C`。systemd 用下面的 `stop`。

## 3. systemd（建议）

`/etc/systemd/system/mint-engine.service`：

```ini
[Unit]
Description=Mint Engine
After=network.target

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/mint-bot
Environment=PATH=/opt/mint-bot/.venv/bin
ExecStart=/opt/mint-bot/.venv/bin/python -m mint_engine
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
```

`User` 换成实际运行用户，并保证对该目录可读写。

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now mint-engine
sudo systemctl status mint-engine
sudo journalctl -u mint-engine -f
```

改 `.env` 后：

```bash
sudo systemctl restart mint-engine
```

## 4. 防火墙与反向代理

进程默认绑 `0.0.0.0:8056`，无登录、CORS `*`。`/api/run/start`、`/api/treasury/distribute/start`、`/api/treasury/collect/start` 和 `/api/sweep/run` 会收到私钥。

最低限度：

- 安全组 / `ufw` 只对你自己的 IP 开放 `8056`，或只对本机开放，外面走 Nginx。
- 不要把控制台裸挂到全网。

Nginx 示例（HTTPS + 长超时，抢跑请求可能挂数小时到数天）：

```nginx
server {
    listen 443 ssl;
    server_name mint.example.com;

    # ssl_certificate     ...
    # ssl_certificate_key ...

    location / {
        proxy_pass http://127.0.0.1:8056;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 7d;
        proxy_send_timeout 7d;
    }
}
```

对应把 `HOST=127.0.0.1`，只让 Nginx 访问引擎。云负载均衡 / CDN 也要把空闲超时调到足够大，否则到点抢跑的 `/api/run/start` 会被掐断（服务端仍可能继续跑到进程退出）。

页面上的「停止」只断开浏览器请求，撤不回已经广播的交易。

## 5. 运行检查

1. 打开 `https://你的域名/` 或 `http://服务器IP:8056/`。
2. 日志应出现「后端已连接」。
3. 「填充示例」→「检测环境」：测速表有结果（填了自有 RPC 就测自有），当前 Gas 开始刷新（Gas 走公开节点）。
4. 「分析合约」能读到协议与 Sale（OpenSea 链接需要 Key 且机器能访问 `api.opensea.io`）。
5. 没有私钥不要点启动或确认归集。
6. 归集：选链和 NFT 合约 → 预览持仓 → 填出售地址 → 确认归集。Robinhood 上加 tip 不会更快。

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| 改了 `.env` 不生效 | 重启进程；`get_settings()` 有缓存 |
| OpenSea 解析 401 / 超时 | 检查 Key；服务器出网是否被墙 |
| OpenSea `/mint` 单次 ~100–250ms | `api.opensea.io` 经 Cloudflare，源站多在海外；德国 VPS 通常已走 FRA/AMS 边缘。引擎使用 **连接池 + T−20 GET 预热**；要再压低 RTT 可对比 **美东** VPS 上 `curl -w time_total` |
| 检测环境全 UNHEALTHY | 自有 RPC 不通或 chain_id 不对；未填自有时检查服务器能否访问公开节点 |
| 启动请求被 504 / 断开 | 反向代理超时太短 |
| ABI 获取失败 | 仍可能靠 bytecode / SeaDrop 读档；可手动粘 ABI |
| 端口被占用 | 改 `PORT`，或停掉旧进程 |

旧版 `quantum/` CLI 与本服务独立，部署控制台不必启动它。
