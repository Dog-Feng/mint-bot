# 通用 EVM Mint Engine — 产品与配置方案

> 文档版本：v1.2  
> 使用与部署：[README.md](./README.md) · [部署.md](./部署.md)  
> 定位：在 [架构设计.md](./架构设计.md) 的引擎分层之上，定义**页面如何配置、如何启动、启动前如何决策、运行中如何并发与加速**。  
> 核心原则：**链必须手选；测速用默认公开 RPC，自有 RPC 只用于分析与广播；钱包各自独立并发；Gas 由用户明确加价；未确认前不广播。**

---

## 1. 产品定义

### 1.1 这是什么

这是一个**本地优先**的通用 EVM Mint 控制台：

```text
用户在页面填写配置
        ↓
后端引擎分析合约、检测销售状态
        ↓
页面展示检测结果与缺口
        ↓
用户确认后启动
        ↓
多钱包并发预签名 / 广播 / 监控
```

它不是链上自动发现一切的黑盒，而是：

> **人工指定运行环境 + 引擎自动识别 mint 模式 + 用户确认后执行。**

### 1.2 成功标准

用户只需要在页面提供：

```text
链
合约地址
数量
RPC 列表
钱包列表
Gas 加价
```

系统负责：

```text
测 RPC → 选最优
识别合约 / 协议 / 方法
读取价格、窗口、限额
计算付款
模拟交易
按钱包并发执行
回传每钱包结果和 Token ID
```

识别不了的部分必须停在页面上，明确写出缺口，而不是盲发。

### 1.3 能力等级

每次检测后，页面必须显示其中一个：

| 等级 | 含义 | 页面行为 |
|---|---|---|
| `AUTO` | 链上信息足够，可直接 mint | 允许一键启动 |
| `SEMI_AUTO` | 还缺 1–2 个已知参数 | 高亮缺口，补全后才能启动 |
| `INTEGRATION_REQUIRED` | 需要 proof / 签名 / 项目 API | 显示集成入口，禁止盲发 |
| `UNSUPPORTED` | 无法安全构造交易 | 只展示诊断，禁用启动 |

对外承诺是：**主流公开 ETH mint 可自动；其余可诊断、可补参、可扩展。**

---

## 2. 设计原则

1. **链由用户指定。** EVM 地址在所有链格式相同，不能从合约地址反推链。
2. **先检查，后启动。** 默认路径是 `检测 → 确认 → Dry Run → 启动`，不是 `输入地址立刻 mint`。
3. **RPC 启动时实测，不永久写死主备。** 多 RPC 先测延迟、高度、可用性，再选主 RPC，其余作广播备份。
4. **多钱包天然隔离。** 每个钱包独立 nonce、独立余额检查、独立签名、独立回执。
5. **Gas 加价必须可见。** 用户提高的是明确的 tip 和 maxFee 倍率，页面要预估费用。无硬顶。
6. **同一份已签名交易可多 RPC 广播。** 这不会变成多笔链上交易。
7. **私钥不进日志、不进结果、不进前端长期存储明文。**
8. **不绕过 allowlist、不伪造签名、不猜测未知加密参数。**

---

## 3. 用户主流程

```text
打开控制台
        ↓
① 选择链
        ↓
② 填写 RPC（可多个）
        ↓
③ 导入钱包
        ↓
④ 填写合约、数量、Gas 加价、启动策略
        ↓
⑤ 点击「检测环境」
        ↓
   测 RPC / 校验链 ID / 读钱包余额
        ↓
⑥ 点击「分析合约」
        ↓
   解 Proxy / 拉 ABI / 识别协议 / 读 Sale
        ↓
⑦ 页面展示检测报告
        ↓
   用户确认方法、价格、缺口、Gas
        ↓
⑧ Dry Run（eth_call，不广播）
        ↓
⑨ 选择启动模式：立即 / 到点抢跑
        ↓
⑩ 启动
        ↓
   预签名 → 广播 → 监控 → 解析 Token
```

任何一步失败，都停在当前步，并给出可操作原因。例如：

```text
RPC 全部不可用
链 ID 与所选链不一致
钱包余额不足
合约不是公开 mint
缺少 merkle proof
Sale 未开始（可预约，不可立即广播）
```

---

## 4. 页面信息架构

建议做成单页控制台，左侧配置，右侧状态。不要做成向导式多页，抢 mint 时需要一眼看到全部关键参数。

```text
┌────────────────────────────┬────────────────────────────┐
│  配置区                    │  检测 / 运行区              │
│                            │                            │
│  链                        │  RPC 测速结果               │
│  RPC 列表                  │  合约分析结果               │
│  钱包列表                  │  Sale 状态                  │
│  合约地址                  │  方法与参数                 │
│  数量                      │  Dry Run 结果               │
│  Gas 策略                  │  钱包准备状态               │
│  启动策略                  │                            │
│                            │  运行日志                   │
│  [检测环境] [分析合约]     │  每钱包结果                 │
│  [Dry Run]  [启动]         │                            │
└────────────────────────────┴────────────────────────────┘
```

按钮状态：

```text
检测环境     随时可点
分析合约     环境检测通过后可点
Dry Run      分析通过且无阻塞缺口后可点
启动         Dry Run 通过，或用户明确选择「跳过模拟但仍确认」后可点
停止         仅运行中可点
```

默认不允许跳过 Dry Run。只有 `AUTO` 且用户勾选「我已知晓风险」才允许跳过。

---

## 5. 配置模型

页面上的全部输入，最终收敛为一份 `RunConfig`。后端只消费这份配置，不另外读取散落的环境变量作为主来源。`.env` 只用于本地默认值，例如浏览器器启动端口、加密密钥。

```yaml
run:
  name: "example-mint"
  mode: inspect          # inspect | dry_run | mint
  start_strategy: scheduled  # immediate | scheduled

chain:
  key: base              # 页面手选
  chain_id: 8453         # 由链预设带出，只读
  native_symbol: ETH

rpc:
  urls:
    - https://base-mainnet.example-1.com
    - https://base-mainnet.example-2.com
    - https://base-mainnet.example-3.com
  probe_on_start: true
  probe_timeout_ms: 1500
  min_healthy: 1
  selection: auto        # auto | manual
  primary: null          # auto 时由测速写入
  broadcast_backups: true

wallets:
  source: keystore       # keystore | pasted
  items:
    - label: wallet_1
      address: "0x..."
    - label: wallet_2
      address: "0x..."
  concurrency: 8
  stop_on_sold_out: true
  skip_if_already_minted: true

mint:
  contract: "0x..."
  quantity: 2
  recipient_mode: self   # self | custom
  abi:
    source: auto         # auto | manual
    json: null           # 用户粘贴的 ABI JSON，填写后优先级最高
  extra_params: {}

gas:
  type: eip1559
  bump:
    mode: extra_tip
    extra_priority_gwei: 1.0   # 0 = 只用链上 tip
    max_fee_multiplier: 1.0    # <=0 当作 1.0
    hard_cap_gwei: 0           # 页面已移除硬顶；0 = 不限制
  gas_limit_mode: estimate_or_fallback
  fallback_gas_limit: 280000
  max_retries: 3               # 页面可改，0–10

schedule:
  start_time_unix: 1730000000
  prepare_lead_sec: 60
  sign_lead_sec: 20
  final_check_lead_sec: 5

safety:
  dry_run_required: true
  allow_skip_simulation: false
  never_log_private_keys: true
```

页面不直接编辑 YAML。YAML 是后端落盘和复用任务时的标准格式；页面是这份结构的表单视图。

---

## 6. 链配置

### 6.1 手动选链

页面提供链下拉，不提供「自动识别链」。

MVP 预设：

| 显示名 | key | chain_id | 默认 Gas 类型 |
|---|---|---|---|
| Ethereum | ethereum | 1 | EIP-1559 |
| Base | base | 8453 | EIP-1559 |
| Arbitrum One | arbitrum | 42161 | EIP-1559 |
| Optimism | optimism | 10 | EIP-1559 |
| BSC | bsc | 56 | 兼容 EIP-1559 / Legacy |
| Polygon | polygon | 137 | EIP-1559 |
| Robinhood Chain | robinhood | 4663 | EIP-1559 |

后续加链只加预设，不改引擎。

Robinhood Chain 补充：

```text
类型            Arbitrum Orbit L2，结算到 Ethereum
原生代币        ETH
公开 RPC        https://rpc.mainnet.chain.robinhood.com
浏览器          https://robinhoodchain.blockscout.com
出块            约 100ms
mempool         无公开 mempool，由 sequencer 先到先得
```

因此 Robinhood 上「多 RPC 同时广播」仍然有效，但 Ethereum 主网那套私有 builder / 抢公共 mempool 的策略不适用。预签名后直接打 sequencer RPC 即可。加 tip **不会更快**；快慢看 RPC 延迟、发出早晚和 sequencer 排队。页面把 tip / 倍率锁为 0，走链上最低有效价。

### 6.2 选链后自动带出

```text
chain_id
native token 符号
区块浏览器
默认 gas 类型
是否有 L1 data fee（OP / Base）
是否建议私有广播（Ethereum 主网预留）
```

用户仍必须自己填 RPC。系统不内置付费 RPC Key，避免把公共额度写死在程序里。

### 6.3 启动时强制校验

每个 RPC 必须返回：

```text
eth_chainId == 所选 chain_id
```

不一致的 RPC 直接标记 `CHAIN_MISMATCH`，不得进入候选。这能避免用户选了 Base、却填了 ETH 主网 RPC。

---

## 7. RPC：多入口、启动测速、动态选主

### 7.1 页面怎么填

RPC 输入是多行文本或标签列表，每行一个 URL：

```text
https://xxx.xxx/xxx
https://yyy.yyy/yyy
https://zzz.zzz/zzz
```

支持：

```text
添加
删除
启用 / 禁用
手动指定主 RPC
一键测速
```

默认策略：`selection = auto`。用户也可以锁定某个 RPC 当主节点。

### 7.2 启动时测什么

「检测环境」和「启动」都会先跑一轮 RPC Probe。每一条 RPC 并行探测：

```text
eth_chainId
eth_blockNumber
eth_gasPrice 或 eth_maxPriorityFeePerGas
eth_getBalance(第一个钱包)   # 顺带验证读权限
往返延迟
```

单条超时建议 `1500ms`，避免一条慢节点拖死整个启动。

### 7.3 健康状态

| 状态 | 条件 |
|---|---|
| `HEALTHY` | 链 ID 正确、延迟可接受、高度不落后 |
| `DEGRADED` | 能通，但延迟高或高度落后 2 个块以上 |
| `UNHEALTHY` | 超时、5xx、解析失败 |
| `CHAIN_MISMATCH` | 通了但 chain_id 不对 |

### 7.4 如何选最优

对 `HEALTHY` / `DEGRADED` 打分：

```text
score =
  0.50 * latency_score
+ 0.25 * block_height_score
+ 0.15 * availability_score
+ 0.10 * error_rate_score
```

规则：

```text
主 RPC     = 分数最高且 HEALTHY 的节点
备份 RPC   = 其余 HEALTHY / DEGRADED
广播集合   = 主 RPC + 备份 RPC
只读集合   = 全部 HEALTHY / DEGRADED
```

用途拆开：

```text
读状态 / 估 Gas / 查回执   → 主 RPC，失败再切备份
广播已签名交易             → 同时打到广播集合
```

不要把「最优 RPC」理解成只留一个。最优用于**决策和读取**；广播仍然可以多发同一笔 raw tx。

### 7.5 页面展示

测速表至少显示：

```text
RPC                  延迟    高度        状态         角色
rpc-1                87ms    21988011    HEALTHY      主
rpc-2                140ms   21988011    HEALTHY      广播备份
rpc-3                920ms   21988004    DEGRADED     仅备份
rpc-4                timeout -           UNHEALTHY    禁用
```

若全部失败：禁止分析、禁止启动，提示先换 RPC。

### 7.6 运行中是否重测

```text
检测环境时：必测
启动前：再测一次
运行中：每 15–30 秒轻量探活，不打断已签名交易
主 RPC 连续失败：自动切下一档，页面提示已切换
```

已签名交易不因切主 RPC 而重签。切的是发送和查询通道。

---

## 8. 钱包：导入、检查、多钱包并发

### 8.1 页面怎么导入

MVP 支持两种本地导入，默认推荐第一种：

```text
1. 加密 Keystore（密码解锁后进入本次运行内存）
2. 粘贴私钥（仅保存在本次会话内存，关闭任务即丢弃）
```

页面展示只显示：

```text
label
address
ETH 余额
nonce
已 mint 数量
准备状态
```

永远不回显私钥。

### 8.2 每个钱包启动前检查

```text
地址合法
私钥能签出该地址
native token 余额 >= 预估 value + 预估 gas
pending nonce 可读
未超过 maxPerWallet
未被标记 skip
```

单钱包失败不应阻断其他钱包。页面上该行标红，其余继续。

### 8.3 并发模型

```text
Wallet A ── 检查 ── 构造 ── 签名 ── 广播 ── 回执
Wallet B ── 检查 ── 构造 ── 签名 ── 广播 ── 回执
Wallet C ── 检查 ── 构造 ── 签名 ── 广播 ── 回执
```

约束：

```text
钱包之间禁止共享 nonce
默认并发数 = min(钱包数, 用户填写的 concurrency)
同一钱包同一时间只有 1 笔 in-flight mint
某个钱包 AlreadyMinted / Wallet Limit → skip
全局 SoldOut / Sale Ended → 取消未广播任务
```

### 8.4 抢跑时的协同

多钱包不是「谁快谁替代谁」，而是「各自为自己抢配额」。

```text
stop_on_sold_out = true
        ↓
任一回执解码为 SoldOut / 供应量耗尽
        ↓
停止尚未广播的钱包
已广播的继续等回执
```

不要在发现 SoldOut 后对已发出交易做无意义重发。

### 8.5 Replace-by-fee

页面提供可选项：

```text
启用卡单加速：是 / 否
加速方式：提高 tip 后替换同一 nonce
最多替换次数：1
```

默认关闭。打开后，只有 `PENDING` 超过用户设定秒数才替换。这和「避免 nonce 冲突」不矛盾：替换必须是同一钱包、同一 nonce、同一笔意图。

---

## 9. Gas：用户加价，系统算账

### 9.1 页面输入

用户不手填 `maxFeePerGas` 原始 wei。控制台「Gas 与启动」三项原样进 `RunConfig.gas`：

```text
额外 tip (gwei)     extra_priority_gwei     默认 1
maxFee 倍率         max_fee_multiplier      默认 1
重试次数            max_retries             默认 3，范围 0–10
```

硬顶已从页面移除。后端仍接受 `hard_cap_gwei`，`0` 表示不限制。

Robinhood（`chain_id=4663`，无公开 mempool）：额外 tip 和倍率锁定为 `0` 并置灰，走链上最低价；重试次数仍可改。

示例（默认值，baseFee=0.8、链上 tip=0.05）：

```text
额外 tip         1 gwei
实际 tip         1.05 gwei
倍率             1
maxFee           0.8 * 1 + 1.05 = 1.85 gwei
```

### 9.2 计算规则

前后端同一套 EIP-1559：

```text
priority = 链上 tip + extra_priority_gwei     # extra ≤ 0 则不加
maxFee   = baseFee * mul + priority           # mul ≤ 0 当作 1.0
gasLimit = estimateGas * 1.2                  # 失败则 280000
```

`SEND_FAILED` / `TIMEOUT` 时同 nonce 重签（`attempt = 1 … max_retries`）：

```text
priority *= 1.5 ^ attempt
mul      *= 1.25 ^ attempt
gasLimit 沿用首次 estimate，不改 calldata 就不重估
```

实付约 `gasUsed × (baseFee + priority)`。同 nonce 替换最终只成交一笔。

### 9.3 Gas Limit

开售前 `eth_estimateGas` 可能因 `NotActive` revert。实现：

```text
每次真正构造交易时（含 T=0）：
    对这一笔 calldata 做 estimateGas × 1.2
    失败则 fallback 280000
重试加价：
    沿用上一档 gasLimit
```

`NOT_STARTED` 不拦截预约；估失败用兜底 limit，到点仍会再估一次。

### 9.4 归集 NFT

mint 完成后把各源钱包的 NFT 转到一个出售地址。预览不看目标地址，只看源钱包是否持有该合约 NFT。

```text
NFT 合约              默认可等于 Mint 合约
归集目标地址          确认归集时必填
转出范围              scan（默认）| last_mint | manual
标准                  auto | erc721 | erc1155
归集并发              默认 2；同钱包串行
每钱包最多转出        0 = 范围内全部
```

扫描顺序：`tokensOfOwner` / `walletOfOwner` → Enumerable → Blockscout NFT 列表 → Transfer 日志。空钱包跳过。确认归集走 `safeTransferFrom`，估失败再试 `transferFrom`。Gas 与重试沿用「Gas 与启动」。

### 9.5 页面必须显示费用预估

在启动按钮上方固定展示：

```text
单钱包 mint value     : 0.002 ETH
单钱包预估 gas        : 0.00021 ETH
单钱包合计            : 0.00221 ETH
钱包数                : 5
最坏总支出            : 0.01105 ETH
```

任一钱包余额不足，对应行标红，并询问：

```text
跳过余额不足钱包并继续
或
中止启动
```

---

## 10. 合约与 Mint 参数

### 10.1 「分析合约」做什么

分析合约不是发交易，而是在启动前把「这个合约怎么 mint」查清楚。

固定顺序：

```text
用户手选的链 + 合约地址
        ↓
解开 Proxy，找到 Implementation
        ↓
解析 ABI
        ↓
识别合约类型 / 协议 / 候选 mint 方法
        ↓
读取 price / 时间窗 / 限额 / 剩余
        ↓
给出能力等级和缺口
```

ABI 是这一步的关键输入。有 ABI，才能知道函数名、参数类型、是否 payable，才能构造 calldata。没有 ABI，只能退到 selector 猜测，很多项目会变成 `SEMI_AUTO` 或 `UNSUPPORTED`。

### 10.2 ABI 从哪来

优先级从高到低：

```text
1. 用户在页面手动粘贴的 ABI
2. 本地 abi_cache
3. Etherscan / Basescan 等浏览器已验证 ABI
4. Sourcify / Blockscout
5. Proxy Implementation 的已验证 ABI
6. Selector 数据库（无完整 ABI）
```

所以：**能分析出 ABI，但不是每次都能自动分析出。**

| 情况 | 结果 |
|---|---|
| 合约已在浏览器开源验证 | 通常能自动拉到完整 ABI |
| 合约是 Proxy | 必须再拉 Implementation ABI |
| 合约未验证 / 私有编译 | 自动获取失败 |
| 用户手动粘贴 ABI | 直接使用，不再依赖浏览器 |

自动获取失败时，页面必须明确写：

```text
ABI 来源：获取失败
能力等级：SEMI_AUTO 或 UNSUPPORTED
请粘贴 ABI 后重新分析
```

而不是假装已经识别出 `mint(uint256)`。

### 10.3 手动 ABI 配置

页面提供：

```text
ABI 来源：自动获取 / 手动粘贴
合约 ABI：JSON 文本框
```

规则：

```text
手动 ABI 只要通过 JSON 解析，就拥有最高优先级
兼容两种粘贴格式：
  1. ABI 数组  [ { "type": "function", ... } ]
  2. 含 abi 字段的对象  { "abi": [ ... ] }
解析后统计 function 数量，并高亮 mint / claim / purchase 候选
无效 JSON 时禁止进入 Dry Run
```

这是给未验证合约、自定义 Sale 合约、以及浏览器 API 限流时的正式入口，不是调试彩蛋。

### 10.4 页面必填

```text
合约地址
数量 quantity
```

手动 ABI 不是必填。只有「ABI 来源 = 手动粘贴」时才必填。

### 10.5 分析后回填，允许用户改

```text
检测到的协议          SeaDrop / Direct / Merkle / Unknown
建议方法              mintPublic(uint256) / mint(uint256) ...
付款                  ETH / ERC-20
单价                  ...
应付 value            price * quantity + protocol fee
开始 / 结束时间
maxPerWallet
remaining
当前阶段              NOT_STARTED / ACTIVE / ENDED / PAUSED
能力等级              AUTO / SEMI_AUTO / ...
```

如果引擎找到多个候选方法，页面列出并按置信度排序，默认选第一名，用户可改选。

### 10.6 缺口补参

`SEMI_AUTO` 时，页面动态出现额外输入，而不是让用户去改代码：

```text
feeRecipient
tokenId
merkle proof
signature + deadline
自定义 recipient
ERC-20 spender approve
```

补齐前，「启动」保持禁用。

### 10.7 不要自动做的事

```text
不自动切换到 onlyOwner / MINTER_ROLE 函数
不自动生成假 proof
不自动调用未知项目的签名接口
不在用户未选链时猜测 chain_id
```

---

## 11. 启动策略

页面提供两种启动方式。

### 11.1 立即启动

适合已经 `ACTIVE` 的公开 mint。

```text
再测 RPC
刷新 Sale
检查钱包
Dry Run（若未跳过）
签名
广播
监控
```

### 11.2 到点抢跑

适合 `NOT_STARTED` 但已有 `startTime` 的项目。

```text
T-20s   取 pending nonce、取 gas、构造、预签名
T-5s    终检 startTime / price / quantity，刷新后重签
T=0     再 estimateGas+20% 并重签，多 RPC 齐射
之后    监控回执；SEND_FAILED / TIMEOUT 按 max_retries 同 nonce 加价重签
```

`T=0` 以开售时间戳为准。到点会再估一次 gasLimit，不是「预签完就不再估」。

如果终检发现价格变了、数量不合法、或 Sale 被暂停，取消广播并在页面告警。

---

## 12. 检测报告（页面右侧主内容）

分析完成后，页面输出一份固定结构的报告。这是用户决定是否启动的依据。

```text
环境
  链            Base (8453)
  主 RPC        rpc-1  87ms
  可用 RPC      3 / 4
  钱包          5 个已解锁，1 个余额不足

合约
  类型          ERC721
  Proxy         是 → implementation 0x...
  协议          SeaDrop
  能力等级      SEMI_AUTO

方法
  建议          mintPublic(address,address,address,uint256)
  置信度        0.96
  候选项        可展开切换

销售
  状态          NOT_STARTED
  单价          0.002 ETH
  数量          2
  应付          0.004 ETH
  开始          2026-09-12 15:00 UTC
  钱包上限      2
  剩余          1200

缺口
  feeRecipient  未确认（已从合约读到候选，请勾选）

Gas
  tip           1.05 gwei
  maxFee        1.85 gwei
  gasLimit      estimate+20%
  重试          3
  单钱包预估    gasLimit × maxFee

钱包
  wallet_1      READY
  wallet_2      READY
  wallet_3      INSUFFICIENT_ETH
  wallet_4      READY
  wallet_5      ALREADY_MINTED
```

只有「无阻塞缺口」且至少 1 个钱包 `READY` 时，启动按钮才亮。

---

## 13. 运行监控

启动后，配置区锁定大部分字段，只保留：

```text
停止
提高 Gas 并替换（若开启）
复制任务日志
```

运行区改为实时表：

```text
钱包        阶段            nonce   tx hash     状态        token ids
wallet_1    broadcast       12      0xabc...    PENDING     -
wallet_2    receipt         4       0xdef...    SUCCESS     881, 882
wallet_3    skipped         -       -           INSUFFICIENT_ETH
```

阶段枚举：

```text
queued
checking
building
signing
waiting_start
broadcasting
pending
success
reverted
skipped
cancelled
timeout
```

日志要求：

```text
记录 RPC 延迟、selector、方法、gas、nonce、模拟结果、回执
不记录私钥、助记词、keystore 密码、原始钱包文件
```

任务结束后，结果写入 `results/`，结构与架构文档中的 Result Schema 一致。

---

## 14. 错误在页面上怎么表现

沿用架构文档的错误分类，但页面要翻译成用户能处理的动作。

| 错误 | 是否可重试 | 页面动作 |
|---|---|---|
| `RPC_ERROR` / 超时 / 429 | 是 | 自动换 RPC，提示已切换 |
| `CHAIN_MISMATCH` | 否 | 停在环境检测 |
| `NOT_STARTED` | 等待 | 允许预约到点启动 |
| `ENDED` / `PAUSED` / `SOLD_OUT` | 否 | 停止未广播任务 |
| `WALLET_LIMIT` / `AlreadyMinted` | 否 | 跳过该钱包 |
| `INSUFFICIENT_ETH` | 否 | 跳过该钱包 |
| `INVALID_PROOF` / `INVALID_SIGNATURE` | 否 | 回到缺口补参 |
| `INVALID_PARAMETER` | 否 | 回到方法/参数确认 |
| `NONCE_ERROR` | 刷新后可 | 重取 pending nonce |
| `GAS_ERROR` | 视情况 | 提示提高额外 tip 或 maxFee 倍率 |
| `REVERTED` | 通常否 | 展示解码后的 revert |
| `TIMEOUT` | 是 | 继续查回执，可选替换 |

---

## 15. 和引擎架构怎么接

页面只做配置与展示。真正工作仍按架构文档三层进行。

```text
Web Console
    │
    │  RunConfig
    ▼
Mint Controller
    │
    ├── RPC Pool / Probe
    ├── Wallet Manager
    ├── Contract Analyzer
    ├── Mint Strategy / Adapter
    ├── Parameter / Call Builder
    ├── Gas / Nonce / Signer
    ├── Broadcaster
    └── Receipt / Result Parser
```

页面调用的后端接口保持很少：

```text
POST /api/rpc/probe
POST /api/wallets/unlock
POST /api/inspect
POST /api/dry-run
POST /api/run/start
POST /api/run/stop
GET  /api/run/{id}/events
GET  /api/run/{id}/result
```

`inspect` 负责分析，不签名。  
`dry-run` 负责 `eth_call`，不广播。  
`run/start` 才进入签名和发送。

这样即使以后把 Web 换成 CLI，同一份 `RunConfig` 仍能用 `scripts/mint.py` 跑。

---

## 16. 推荐落地形态

### 16.1 为什么是本地 Web，而不是纯 CLI

你的使用方式是「页面配置后启动」。本地 Web 的好处：

```text
多 RPC、多钱包、Gas、检测报告更适合表格展示
启动前确认比命令行更不容易发错
运行中每个钱包状态能一眼看到
```

但仍必须是**本机后端**：

```text
浏览器只谈 localhost
私钥只在本机内存解锁
不把私钥上传到远程服务器
```

### 16.2 技术栈

与架构文档对齐，不另起一套语言：

```text
后端    Python 3.11+
        FastAPI
        web3.py / eth-account / eth-abi
        httpx + asyncio
        pydantic（RunConfig / API）

前端    先用 FastAPI + 轻量 HTML/JS 单页
        配置复杂后再拆独立前端

存储    SQLite
        abi_cache/
        results/
        加密 keystore 目录
```

MVP 不必上 Redis。RPC 延迟、ABI、implementation 用内存 + SQLite 即可。

### 16.3 目录补充

在架构文档目录上增加 Web 与任务层：

```text
mint_engine/
├── web/
│   ├── app.py
│   ├── api.py
│   └── static/
├── config/
│   ├── chains.py
│   ├── run_config.py
│   └── presets.py
├── rpc/
│   ├── pool.py
│   ├── probe.py
│   └── broadcaster.py
├── wallet/
│   ├── manager.py
│   └── keystore.py
└── scripts/
    ├── inspect_contract.py
    ├── dry_run.py
    └── mint.py
```

CLI 和页面共用同一套 Controller。页面不是另一套 mint 逻辑。

---

## 17. 安全边界（产品层）

```text
私钥
  只存在本次解锁后的内存
  不写 results、日志、异常、前端 localStorage 明文

RPC
  用户自备
  系统不把用户 RPC Key 打印到报告下载文件以外的地方

交易
  同一 raw tx 多播
  不自动给未知 spender 无限 approve，除非用户确认

权限
  检测出 onlyOwner / MINTER_ROLE 时标记 PUBLIC_MINT_UNSUPPORTED
  不提供「强制发送」到这类函数的快捷按钮
```

---

## 18. MVP 范围

第一期页面只做下面这些，做完就能真实使用。

### 必须有

```text
手选链
多 RPC 输入 + 启动测速 + 选主
多钱包导入与并发
quantity
Gas 加价（额外 tip + maxFee 倍率 + 重试次数）
合约分析报告
Dry Run
立即启动
到点抢跑（有 startTime 时）
每钱包结果与日志
归集 NFT（预览持仓 + 转到出售地址）
```

### 第一期只自动支持

```text
已验证 ABI 的公开 ETH mint
mint(uint256)
mint(address,uint256)
publicMint(uint256)
SeaDrop mintPublic
```

### 第一期明确不做

```text
自动猜链
ABI-less 启发式盲发
Bytecode 深度分析
自动破解 allowlist
远程托管私钥
公共前端 + 云端代签
ERC-20 作为主付款（可后置）
Flashbots 私有通道（Ethereum 预留配置位，但不做进 MVP）
```

---

## 19. 完整运行时序

把页面配置和引擎执行合成一条时间线：

```text
用户手选链、填 RPC、导入钱包、填合约和数量、设 Gas 加价
        ↓
[检测环境]
  并行探测全部 RPC
  过滤 chain_id 不一致节点
  选主 RPC
  读每个钱包余额和 nonce
        ↓
[分析合约]
  Proxy → Implementation
  ABI
  协议指纹
  候选 mint 方法
  SaleState（价格、窗口、限额、剩余）
  计算 value
  给出能力等级和缺口
        ↓
用户确认方法 / 补参数 / 确认费用
        ↓
[Dry Run]
  对 1 个 READY 钱包 eth_call
  开售前允许因 NotActive 失败，但 calldata 必须能编出来
        ↓
[启动]
  immediate:
      全钱包并发签名并广播
  scheduled:
      T-60 再准备
      T-20 预签名
      T-5  终检
      T=0  多 RPC 广播
        ↓
[监控]
  多 RPC 查回执
  解析 Transfer / TransferSingle
  SoldOut 则停未发钱包
        ↓
[结束]
  汇总 SUCCESS / REVERTED / SKIPPED
  写入 results/
```

---

## 20. 最终产品形态

用户打开页面后，完整心智模型应是：

```text
我指定在哪条链跑
我指定用哪些 RPC
我指定用哪些钱包
我指定 mint 谁、mint 几个
我指定愿意多付多少 gas
系统负责测速、分析、算钱、模拟、并发发送和回收结果
凡是它吃不准的，必须在页面上问我
```

一句话：

> **通用 Mint Bot 的产品入口是「可确认的控制台」，不是「只收一个合约地址的自动按钮」。**

引擎负责回答「这个合约怎么 mint」。  
页面负责让用户回答「在哪条链、用哪些节点、哪些钱包、加多少 gas、现在能不能发」。  
两者合在一起，才是可启动的完整方案。
